20. Definition

    - defines prospective fine-tuning
    - a simple way \ perhaps
      in contrast to retrospective fine-tuning
       - rather than
         tuning in retrospect (and for sake) of an object already attained,
         tuning *in prospect* (and for sake) of an object *yet unattained*
